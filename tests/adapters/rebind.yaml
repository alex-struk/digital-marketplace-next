rebind:
  - id: R-2.1
    target: old
    why: publishOpportunity's opportunityCwuCreate.publish(input) fills every field
      the test gives, but the old form's '3. Details' step also requires
      'Acceptance Criteria*' and 'Evaluation Criteria*', which the contract's
      input never carries; the snapshot shows both blank, so Publish stays
      disabled and press() throws before the proposal is reached. Enter valid
      values into required fields the input does not name before pressing
      Publish.
  - id: R-2.2
    target: old
    why: publishOpportunity's opportunityCwuCreate.publish(input) leaves the
      required 'Acceptance Criteria*' and 'Evaluation Criteria*' fields on '3.
      Details' blank because the contract's input never names them, so Publish
      stays disabled and press() throws before the second proposal is attempted.
      Fill required fields the input does not name before pressing Publish.
  - id: R-2.3
    target: old
    why: publishOpportunity's opportunityCwuCreate.publish(input) leaves the
      required 'Acceptance Criteria*' and 'Evaluation Criteria*' on '3. Details'
      blank (the contract's input never names them), so Publish stays disabled
      and press() throws before any proposal is submitted. Fill required fields
      the input does not name before pressing Publish.
  - id: R-1.4
    target: old
    why: opportunityCwuCreate.publish(input) leaves the required 'Acceptance
      Criteria*' and 'Evaluation Criteria*' on '3. Details' blank because the
      contract's input never names them, so Publish stays disabled and press()
      throws before any edit is made. Fill required fields the input does not
      name before pressing Publish.
  - id: R-2.4
    target: old
    why: publishOpportunity's opportunityCwuCreate.publish(input) leaves the
      required 'Acceptance Criteria*' and 'Evaluation Criteria*' on '3. Details'
      blank (not in the contract's input), so Publish stays disabled and press()
      throws before any proposal exists to delete. Fill required fields the
      input does not name before pressing Publish.
  - id: R-2.7
    target: old
    why: opportunityCwuCreate.publish(input) leaves the required 'Acceptance
      Criteria*' and 'Evaluation Criteria*' on '3. Details' blank because the
      contract's input never names them, so Publish stays disabled and press()
      throws before any proposal is created. Fill required fields the input does
      not name before pressing Publish.
  - id: R-2.9
    target: old
    why: opportunityCwuCreate.publish(input) leaves the required 'Acceptance
      Criteria*' and 'Evaluation Criteria*' on '3. Details' blank because the
      contract's input never names them, so Publish stays disabled and press()
      throws before any proposal history exists. Fill required fields the input
      does not name before pressing Publish.
  - id: R-1.10
    target: old
    why: in all six tests opportunityCwuCreate.publish(input) leaves the required
      'Acceptance Criteria*' and 'Evaluation Criteria*' on '3. Details' blank
      (the contract's input never names them), so Publish is disabled whatever
      the field under test holds, and press() throws instead of letting
      fieldError() be read. Fill required fields the input does not name, and
      when Publish is still disabled after that, return so the test can read the
      refusal.
  - id: R-6.10
    target: old
    why: organizationEdit.addTeamMembers({user}) reads only top-level 'emails' or
      'email' keys, so the address nested under 'user' is dropped, nothing is
      typed into 'Email Addresses', the dialog is never confirmed and no
      invitation is sent. Read the email from a nested user record.
  - id: R-1.11
    target: old
    why: in all three tests opportunityCwuCreate.publish(input) leaves the required
      'Acceptance Criteria*' and 'Evaluation Criteria*' on '3. Details' blank,
      so Publish is disabled regardless of the remote-work fields under test,
      and press() throws instead of letting fieldError() be read. Fill required
      fields the input does not name, and return on a still-disabled Publish so
      the refusal can be read.
  - id: R-1.12
    target: old
    why: in all three tests opportunityCwuCreate.publish(input) leaves the required
      'Acceptance Criteria*' and 'Evaluation Criteria*' on '3. Details' blank,
      so Publish is disabled regardless of the reward or skills under test, and
      press() throws instead of letting fieldError() be read. Fill required
      fields the input does not name, and return on a still-disabled Publish so
      the refusal can be read.
  - id: R-2.12
    target: old
    why: publishOpportunity's opportunityCwuCreate.publish(input) leaves the
      required 'Acceptance Criteria*' and 'Evaluation Criteria*' on '3. Details'
      blank (not in the contract's input), so Publish stays disabled and press()
      throws before the draft proposal is saved. Fill required fields the input
      does not name before pressing Publish.
  - id: R-3.12
    target: old
    why: acceptOrgAdminTerms() throws 'no dialog is open' because the test calls it
      before anything has raised the terms dialog; open it from the member's
      Admin box, or return when none is open. toggleMemberAdminStatus({member})
      gets '' from asText for the nested seed user, so it falls back to the
      first checkbox on the Team tab, the owner's checked and disabled box, and
      clicks it until the 15 s timeout. Find the row by the member's name, and
      treat a disabled box as the refusal rather than clicking it.
  - id: R-4.12
    target: old
    why: userProfile.toggleAdminPermission() throws unbound when a vendor's profile
      offers no Admin box (vendor.one's page shows Status, Account Type and no
      Permission(s)), although that absence is the refusal the test goes on to
      read; return quietly as opportunityCreate does for a withheld control. In
      the first test, after the tick and a reopen, the Admin box reads
      unchecked; confirm that the click was saved (wait for any confirmation or
      save) before the page is reopened.
  - id: R-2.13
    target: old
    why: publishOpportunity's opportunityCwuCreate.publish(input) leaves the
      required 'Acceptance Criteria*' and 'Evaluation Criteria*' on '3. Details'
      blank (not in the contract's input), so Publish stays disabled and press()
      throws before any proposal is submitted. Fill required fields the input
      does not name before pressing Publish.
  - id: R-8.13
    target: old
    why: after editOrganization and chooseImage, organizationEdit.saveChanges()
      presses 'Save Changes' but never confirms the 'Save Changes?' dialog it
      raises (seen open in R-3.19), so the snapshot is still in edit mode with
      no stored logo and storedImageWidth() reads nothing (NaN). Confirm the
      dialog and wait for the save before reading the stored image.
  - id: R-1.14
    target: old
    why: in all five tests opportunityCwuCreate.publish(input) leaves the required
      'Acceptance Criteria*' and 'Evaluation Criteria*' on '3. Details' blank,
      so Publish is disabled regardless of the dates under test, and press()
      throws instead of letting fieldError() or the stored dates be read. Fill
      required fields the input does not name, and return on a still-disabled
      Publish so the refusal can be read.
  - id: R-2.14
    target: old
    why: publishOpportunity's opportunityCwuCreate.publish(input) leaves the
      required 'Acceptance Criteria*' and 'Evaluation Criteria*' on '3. Details'
      blank (not in the contract's input), so Publish stays disabled and press()
      throws before any proponent is chosen. Fill required fields the input does
      not name before pressing Publish.
  - id: R-6.17
    target: old
    why: "in 'the watch itself is retained', opportunityCwuCreate.publish(input)
      leaves the required 'Acceptance Criteria*' and 'Evaluation Criteria*' on
      '3. Details' blank, so Publish stays disabled and press() throws. Fill
      required fields the input does not name before pressing Publish. The first
      test's missing 'Deactivate Account' is not a binding fault: that account
      (vendor.terms.reset) already shows Status Inactive with only 'Reactivate
      Account'."
  - id: R-3.18
    target: old
    why: organizationEdit.organizationTab() returns tabContent(['Organization']),
      which drops the top bar, so for the owner the reading lacks the 'Edit
      Organization' control the snapshot shows there and 'Edit' is not found.
      Include the screen's top-bar controls in this reading, as profileTab now
      does.
  - id: R-1.19
    target: old
    why: opportunityCwuCreate.saveDraft and publish(input) leave the required
      'Acceptance Criteria*' and 'Evaluation Criteria*' on '3. Details' blank
      because the contract's input never names them. The draft is therefore
      incomplete, its Actions menu offers Publish disabled and no 'Submit for
      Review', and the create page's Publish throws disabled. Fill required
      fields the input does not name before saving or publishing.
  - id: R-3.19
    target: old
    why: organizationEdit.saveChanges(input) fills the phone number and presses
      'Save Changes', but never confirms the 'Save Changes?' dialog that opens;
      the dialog is still open in the snapshot and intercepts the next click on
      the Organization tab for 15 s. Confirm the dialog with its 'Save Changes'
      control and wait for the save to finish.
  - id: R-1.20
    target: old
    why: in both tests opportunityCwuCreate.publish(input) leaves the required
      'Acceptance Criteria*' and 'Evaluation Criteria*' on '3. Details' blank
      (not in the contract's input), so Publish stays disabled and press()
      throws before any state change is requested. Fill required fields the
      input does not name before pressing Publish.
  - id: R-8.20
    target: old
    why: the Code With Us and proposal tests fail because
      opportunityCwuCreate.publish(input) leaves the required 'Acceptance
      Criteria*' and 'Evaluation Criteria*' blank, so Publish stays disabled.
      opportunitySwuCreate.addPhase maps none of 'startDate', 'completionDate',
      'maxBudget' or 'capabilities' to the phase step's fields, and
      opportunityTwuCreate.addResource has no mapping for 'order'; map those
      keys to the phase and resource fields on those steps.
  - id: R-3.21
    target: old
    why: "for the vendor who neither owns nor administers it,
      organizationList.ownerName() returns '—', the placeholder the Owner cell
      shows when the owner is withheld; read that placeholder as ''. The public
      sector staff test is not a binding fault: that list shows owner names such
      as 'Larkin Placeholder' in links."
  - id: R-1.22
    target: old
    why: opportunityCwuCreate.saveDraft(input) leaves the required 'Acceptance
      Criteria*' and 'Evaluation Criteria*' blank, so the draft is incomplete
      and its Actions menu shows Publish disabled for that reason.
      opportunityCwuEdit.publish() then throws on the disabled entry instead of
      letting the unchanged status be read. Fill required fields the input does
      not name, and treat a disabled Publish entry as the refusal and return.
  - id: R-7.22
    target: old
    why: in both tests contentCreate.publishPage() clicked the enabled top-bar
      Publish, but no confirmation dialog appeared and the page stayed on
      /content/create with every field filled, even for the unique slug
      'derived-rename-clash-…'. confirmPublish therefore finds no dialog, and
      duplicateSlugError() reads before anything was submitted. Make sure the
      click reaches the Publish anchor itself, and wait for the confirmation
      dialog before returning.
  - id: R-1.23
    target: old
    why: opportunityCwuCreate.publish(input) leaves the required 'Acceptance
      Criteria*' and 'Evaluation Criteria*' on '3. Details' blank because the
      contract's input never names them, so Publish stays disabled and press()
      throws before anything is published. Fill required fields the input does
      not name before pressing Publish.
  - id: R-2.23
    target: old
    why: publishOpportunity's opportunityCwuCreate.publish(input) leaves the
      required 'Acceptance Criteria*' and 'Evaluation Criteria*' on '3. Details'
      blank (not in the contract's input), so Publish stays disabled and press()
      throws before any proposal is withdrawn. Fill required fields the input
      does not name before pressing Publish.
  - id: R-2.24
    target: old
    why: opportunityCwuCreate.publish(input) leaves the required 'Acceptance
      Criteria*' and 'Evaluation Criteria*' on '3. Details' blank because the
      input never names them, so Publish stays disabled and press() throws
      before any proposal exists; opportunityTwuCreate.addResource also has no
      mapping for 'order'. Fill required fields the input does not name before
      pressing Publish, and map 'order' on the resource step.
  - id: R-8.25
    target: old
    why: fileAttachmentControl.addAttachment opens the draft's edit page
      (tab=opportunity), which is read-only and shows 'This opportunity is a
      draft. Please select "Edit" from the Actions dropdown'; the adapter walks
      to '4. Attachments' without choosing Edit, so 'Add Attachment' never
      appears. Choose 'Edit' from the Actions menu before looking for 'Add
      Attachment'. opportunitySwuCreate.addPhase also leaves 'startDate',
      'completionDate', 'maxBudget' and 'capabilities' unmapped, and
      opportunityCwuCreate.publish leaves 'Acceptance Criteria*' and 'Evaluation
      Criteria*' blank so Publish stays disabled.
  - id: R-4.27
    target: old
    why: after the name is cleared, or given a name or job title over one hundred
      characters, the profile's 'Save Changes' is disabled and that is the
      refusal the test goes on to read; userProfileSelf.saveChanges press()
      throws on the disabled control instead. When 'Save Changes' is disabled
      after the input is entered, return without saving so fieldError() and the
      reopened fields can be read.
  - id: R-1.28
    target: old
    why: opportunityCwuCreate.publish and saveDraft leave the required 'Acceptance
      Criteria*' and 'Evaluation Criteria*' blank, so the create page's Publish
      and the draft's Actions-menu Publish are disabled and press() throws. For
      the draft, opportunityCwuEdit.cancelOpportunity throws unbound when the
      Actions menu offers no 'Cancel', although that absence is the refusal the
      test reads; return quietly when the action is not offered. Fill required
      fields the input does not name before publishing.
  - id: R-3.28
    target: old
    why: organizationEdit.saveServiceAreas presses the top-bar 'Save Changes' but
      never confirms the 'Are you sure?' dialog it raises ('Please confirm your
      changes to the Service Area list'), so the dialog stays open and
      intercepts the click on the 'TWU Qualification' tab for 15 s.
      saveServiceAreas also ignores its serviceAreas input and never ticks or
      clears boxes. Set the boxes from the input, confirm the dialog with its
      'Save Changes', and wait for it to close.
  - id: R-8.28
    target: old
    why: in the logo test organizationEdit.saveChanges() presses 'Save Changes' but
      never confirms the 'Save Changes?' dialog, which is still open in the
      snapshot with the form in edit mode, so imageAddress() reads ''. Confirm
      the dialog and wait for the save to finish before reading the stored image
      address.
  - id: R-3.30
    target: old
    why: organizationEdit.fieldError() returns '' while the page shows the alert
      'Unable to Add Unregistered Team Members' with
      nobody.registered.here@example.test as a list item under it; messages()
      discards list items as prose and its pattern does not match an address.
      Read the text of a visible alert, list items included, as the warning.
  - id: R-1.31
    target: old
    why: proposalCwuCreate.chooseProponentIndividual() leaves the required 'Legal
      Name*', 'Email Address*', 'Street Address*', 'City*', 'Province / State*',
      'Postal / ZIP Code*' and 'Country*' blank because the input names none of
      them, and acceptProgramTerms then opens the terms through the top-bar
      'Submit', which is disabled, so press() throws before any proposal is
      submitted. Fill required proponent fields the input does not name, and
      reach the terms without a disabled control.
  - id: R-8.31
    target: old
    why: opportunityCwuCreate.publish(input) leaves the required 'Acceptance
      Criteria*' and 'Evaluation Criteria*' blank, so Publish stays disabled and
      press() throws; for the draft, fileAttachmentControl.addAttachment never
      chooses 'Edit' from the Actions menu on the read-only draft page, so 'Add
      Attachment' is never reached. Fill required fields the input does not
      name, and enter editing before attaching.
  - id: R-1.32
    target: old
    why: opportunityCwuCreate.publish and saveDraft leave the required 'Acceptance
      Criteria*' and 'Evaluation Criteria*' blank, so the create page's Publish
      and the draft's Actions-menu Publish are disabled and press() throws. For
      the draft, opportunityCwuEdit.addAddendum throws unbound when no 'Addenda'
      tab is offered, although that absence is the refusal the test reads;
      return quietly. addAddendum also never confirms the 'Publish Addendum?'
      dialog (seen open in R-1.35).
  - id: R-1.34
    target: old
    why: opportunityCwuCreate.saveDraft(input) leaves the required 'Acceptance
      Criteria*' and 'Evaluation Criteria*' blank because the input never names
      them, so the draft is incomplete and opportunityCwuEdit.publish finds the
      Actions-menu Publish disabled and throws. Fill required fields the input
      does not name before saving the draft.
  - id: R-2.34
    target: old
    why: "in the first test the 'Disqualification Reason' dialog is open with Reason
      empty and its 'Disqualify' disabled, which is the refusal the test reads;
      disqualify() press() throws instead. When the dialog's 'Disqualify' is
      still disabled after the reason is entered, cancel the dialog and return.
      The second test's failure is not a binding fault: the page shows the
      proposal as Non-compliant and says its history will be available once the
      opportunity reaches the Challenge."
  - id: R-1.35
    target: old
    why: "in the addendum test opportunityCwuEdit.addAddendum types the text and
      presses 'Publish Addendum', but never confirms the 'Publish Addendum?'
      dialog it raises, which is still open in the snapshot, so no addendum is
      published and no notice is sent. Confirm the dialog and wait for it to
      close. The edit test is not a binding fault: 'Opportunity Changes
      Published' rendered and the author received no mail."
  - id: R-2.35
    target: old
    why: publishOpportunity's opportunityCwuCreate.publish(input) leaves the
      required 'Acceptance Criteria*' and 'Evaluation Criteria*' on '3. Details'
      blank because the input never names them, so Publish stays disabled and
      press() throws before any proposal exists. Fill required fields the input
      does not name before pressing Publish.
  - id: R-1.36
    target: old
    why: opportunityCwuCreate.saveDraft(input) leaves the required 'Acceptance
      Criteria*' and 'Evaluation Criteria*' blank, so the draft is incomplete
      and opportunityCwuEdit.publish finds the Actions-menu Publish disabled and
      throws before anything can be cancelled. Fill required fields the input
      does not name before saving the draft.
  - id: R-2.36
    target: old
    why: in both tests opportunityCwuCreate.publish(input) leaves the required
      'Acceptance Criteria*' and 'Evaluation Criteria*' blank, so Publish stays
      disabled and press() throws before any proposal is submitted or withdrawn.
      Fill required fields the input does not name before pressing Publish.
  - id: R-1.37
    target: old
    why: opportunityCwuCreate.submitForReview(input) leaves the required 'Acceptance
      Criteria*' and 'Evaluation Criteria*' on '3. Details' blank because the
      input never names them; the snapshot shows the top bar with 'Publish'
      disabled and no 'Submit for Review', so offered() returns quietly and
      nothing is submitted. Fill required fields the input does not name before
      looking for 'Submit for Review'.
  - id: R-2.37
    target: old
    why: opportunityCwuCreate.publish(input) leaves the required 'Acceptance
      Criteria*' and 'Evaluation Criteria*' blank, so Publish stays disabled and
      press() throws; opportunitySwuCreate.addPhase leaves 'startDate',
      'completionDate', 'maxBudget' and 'capabilities' unmapped to the phase
      step's fields. Fill required fields the input does not name, and map those
      phase keys.
  - id: R-1.38
    target: old
    why: opportunityCwuCreate.publish(input) leaves the required 'Acceptance
      Criteria*' and 'Evaluation Criteria*' blank, so Publish stays disabled and
      press() throws before any open opportunity exists to order. Fill required
      fields the input does not name before pressing Publish.
  - id: R-2.38
    target: old
    why: publishOpportunity's opportunityCwuCreate.publish(input) leaves the
      required 'Acceptance Criteria*' and 'Evaluation Criteria*' blank, so
      Publish stays disabled and press() throws before any proposal exists to
      export. Fill required fields the input does not name before pressing
      Publish.
  - id: R-1.39
    target: old
    why: in all four tests opportunityCwuCreate.publish(input) leaves the required
      'Acceptance Criteria*' and 'Evaluation Criteria*' blank, so Publish stays
      disabled and press() throws before the list can be filtered. Fill required
      fields the input does not name before pressing Publish.
  - id: R-1.48
    target: old
    why: opportunityCwuCreate.publish and submitForReview leave the required
      'Acceptance Criteria*' and 'Evaluation Criteria*' blank, so Publish throws
      disabled and submitForReview returns without submitting because no 'Submit
      for Review' is shown. opportunitySwuCreate.saveDraft({title}) clicks 'Save
      Draft', but the page stays on /opportunities/sprint-with-us/create, and
      landOn swallows its 20 s timeout, so no Sprint With Us draft exists to
      list. Fill required fields the input does not name, and when Save Draft
      does not reach the record's address, report the page's state instead of
      returning.
  - id: R-1.53
    target: old
    why: opportunitySwuCreate.saveDraft({title}) clicks 'Save Draft', but the
      snapshot is still on /opportunities/sprint-with-us/create with the title
      entered, and landOn swallows its timeout, so
      opportunitySwuEdit.opportunityIdentifier() throws.
      opportunityCwuCreate.submitForReview and publish leave the required
      'Acceptance Criteria*' and 'Evaluation Criteria*' blank, so nothing is
      submitted, the address stays /create, and Publish throws disabled. Fill
      required fields the input does not name, and fail at the save when the
      record's address is never reached.
  - id: R-1.56
    target: old
    why: opportunityCwuCreate.publish and saveDraft leave the required 'Acceptance
      Criteria*' and 'Evaluation Criteria*' blank, so the create page's Publish
      and the draft's Actions-menu Publish are disabled and press() throws.
      opportunitySwuCreate.addPhase leaves 'startDate', 'completionDate' and
      'maxBudget' unmapped, and opportunityTwuCreate.addResourceQuestion has no
      mapping for 'order'. Fill required fields the input does not name, and map
      those keys on the phase and resource question steps.
