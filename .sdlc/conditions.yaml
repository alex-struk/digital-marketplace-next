conditions:
  - ref: build-slice-1-4#1
    text: "The account reports R-7.12 and R-7.17 as criteria this slice treated as
      its own to answer for and exercised. verify records both as never asserted
      against the application, and what stands behind the claim is one
      hand-driven session nobody can repeat: no Docker, no browser, and a
      stand-in database that dropped its connection and answered a request with
      a 500 mid-run. Driving the running application was the right response to
      the last ruling and what it found reads as correct; presenting it as
      having exercised a criterion is not, because a criterion is asserted by
      the acceptance suite and by nothing else. In the next revision's account,
      say what the session showed and say plainly that neither criterion is
      asserted. Keep what that session put into the tree:
      app/frontend/tests/content-view.test.tsx pins the seeded page's rendered
      wording and both of its dates, and
      app/frontend/tests/formatted-text.test.tsx is where the never-executed
      behaviour is held."
    from: build-slice-1-4
    family: build-slice-1
    gate: G3
    stage: build
    by: agent:reviewer
    at: 2026-09-21T23:10:55.272Z
    closed:
      outcome: met
      why: the build-slice-1-6 proposal account says what the hand-driven session
        showed and states that neither R-7.12 nor R-7.17 is asserted;
        app/frontend/tests/content-view.test.tsx and
        app/frontend/tests/formatted-text.test.tsx remain in the tree unchanged.
      by: agent:reviewer
      at: 2026-09-25T02:15:02.486Z
  - ref: build-slice-1-5#1
    text: "The application must answer on the address .sdlc/config.yaml gives the
      new target, http://localhost:4300: the host port app/compose/compose.yaml
      publishes for the frontend, the redirectUris, webOrigins and
      post.logout.redirect.uris in app/compose/idp/realm-template.json, the
      addresses stated in app/README.md and app/compose/idp/README.md, and the
      port asserted in app/backend/tests/sandbox-composition.test.ts. The port
      inside the container is the build's own choice."
    from: build-slice-1-5
    family: build-slice-1
    gate: G3
    stage: build
    by: tech-lead
    at: 2026-09-25T01:57:15.904Z
    closed:
      outcome: met
      why: app/compose/compose.yaml publishes the frontend as 4300:3000; redirectUris,
        webOrigins and post.logout.redirect.uris in
        app/compose/idp/realm-template.json are http://localhost:4300;
        app/README.md and app/compose/idp/README.md state 4300;
        app/backend/tests/sandbox-composition.test.ts asserts the frontend is
        the only service on host port 4300. The dev server in
        app/frontend/vite.config.ts also defaults to 4300, so the sign-in
        redirects agree on a builder's machine.
      by: agent:reviewer
      at: 2026-09-25T02:15:02.483Z
