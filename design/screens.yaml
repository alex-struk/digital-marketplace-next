screens:
  # users domain — see design/DESIGN.md, "Domain: users"
  - page: user-sign-in
    states: [default]
  - page: user-sign-up-choose-account
    states: [default]
  - page: user-sign-up-complete
    states: [default, terms-accepted, invalid, save-failed]
  - page: user-sign-out
    states: [default, loading, failed]
  - page: user-notice
    states: [default, sign-in-failed, not-found]
  - page: user-list
    states: [default, loading, export-open, export-ready, not-found]
  - page: user-profile
    states:
      [
        default,
        loading,
        not-found,
        own,
        editing,
        invalid,
        admin-refused,
        deactivate-confirm,
        deactivated-by-admin,
        reactivate-confirm,
        deactivated-by-owner,
      ]
  - page: user-profile-capabilities
    states: [default, section-unavailable]
  - page: user-profile-notifications
    states: [default, unsubscribe-confirm, section-unavailable]
  - page: user-profile-legal
    states: [default, terms-updated, accept-terms-confirm, section-unavailable]
  - page: user-profile-self
    states:
      [
        default,
        public-sector,
        administrator,
        loading,
        editing,
        invalid,
        save-failed,
        deactivate-confirm,
        sign-in-required,
      ]
  - page: user-profile-self-capabilities
    states: [default, section-unavailable]
  - page: user-profile-self-notifications
    states: [default, unsubscribe-confirm]
  - page: user-profile-self-legal
    states: [default, terms-updated, accept-terms-confirm, section-unavailable]

  # opportunities domain — see design/DESIGN.md, "Domain: opportunities"
  - page: home
    states: [default, loading]
  - page: opportunity-dashboard
    states: [default, administrator, empty, loading]
  - page: opportunity-list
    states: [default, staff, signed-out, loading]
  - page: opportunity-program-select
    states: [default, not-found]
  - page: opportunity-cwu-create
    states: [default, administrator, invalid, publish-confirm, not-found]
  - page: opportunity-cwu-view
    states: [default, signed-out, author, awarded, not-found, loading]
  - page: opportunity-cwu-edit
    states:
      [
        default,
        draft,
        under-review,
        editing,
        incomplete,
        addenda-tab,
        history-tab,
        publish-confirm,
        cancel-confirm,
        delete-confirm,
        not-found,
        loading,
      ]
  - page: opportunity-cwu-complete
    states: [default, not-found, loading]
  - page: opportunity-swu-create
    states: [default, administrator, invalid, publish-confirm, not-found]
  - page: opportunity-swu-view
    states: [default, signed-out, author, awarded, not-found, loading]
  - page: opportunity-swu-edit
    states:
      [
        default,
        draft,
        under-review,
        editing,
        incomplete,
        addenda-tab,
        history-tab,
        publish-confirm,
        cancel-confirm,
        delete-confirm,
        consensus,
        consensus-refused,
        code-challenge,
        team-scenario-refused,
        not-found,
        loading,
      ]
  - page: opportunity-swu-complete
    states: [default, not-found, loading]
  - page: opportunity-twu-create
    states: [default, administrator, invalid, publish-confirm, not-found]
  - page: opportunity-twu-view
    states: [default, signed-out, author, awarded, not-found, loading]
  - page: opportunity-twu-edit
    states:
      [
        default,
        draft,
        under-review,
        editing,
        incomplete,
        addenda-tab,
        history-tab,
        publish-confirm,
        cancel-confirm,
        delete-confirm,
        consensus,
        consensus-refused,
        not-found,
        loading,
      ]
  - page: opportunity-twu-complete
    states: [default, not-found, loading]
  - page: scheduled-transition-trigger
    states: [default]

  # organizations domain — see design/DESIGN.md, "Domain: organizations"
  - page: organization-list
    states: [default, administrator, signed-out, loading, refused]
  - page: organization-create
    states: [default, invalid, ready, not-found]
  - page: organization-edit
    states:
      [
        default,
        org-admin,
        editing,
        invalid,
        archive-confirm,
        team,
        team-administrator,
        invite-open,
        invite-refused,
        invite-invalid-type,
        invite-unregistered,
        admin-rights-confirm,
        remove-member-confirm,
        change-owner,
        swu-qualification,
        swu-qualified,
        twu-qualification,
        twu-administrator,
        service-areas-editing,
        changelog,
        not-found,
        loading,
      ]
  - page: organization-swu-terms
    states: [default, accepted, administrator]
  - page: organization-twu-terms
    states: [default, accepted, administrator]
  - page: organization-user-memberships
    states: [default, empty, loading, accept-confirm, decline-confirm, leave-confirm, section-unavailable]
  - page: organization-user-memberships-self
    states: [default, empty, loading, accept-confirm, decline-confirm, leave-confirm, section-unavailable]

  # notifications domain — see design/DESIGN.md, "Domain: notifications"
  - page: notification-unsubscribe-landing
    states: [default, loading, unsubscribed, sign-in-required]
  - page: notification-optin-opportunity-list
    states: [default, subscribed, signed-out]
  - page: notification-terms-broadcast
    states: [default, notify-confirm, notified, notify-failed]
  - page: notification-email-reference
    states: [default, loading, not-found]

  # content domain — see design/DESIGN.md, "Domain: content"
  - page: content-footer
    states: [default]
  - page: content-service-level-agreement-link
    states: [default]
  - page: content-list
    states: [default, deleted, loading, not-found]
  - page: content-create
    states: [default, invalid, ready, publish-confirm, duplicate-slug, not-found]
  - page: content-edit
    states:
      [
        default,
        fixed,
        created,
        editing,
        editing-fixed,
        invalid,
        duplicate-slug,
        publish-confirm,
        changes-published,
        delete-confirm,
        loading,
        not-found,
      ]
  - page: content-view
    states: [default, loading, not-found]

  # files domain — see design/DESIGN.md, "Domain: files"
  - page: file-upload
    states: [default, signed-out, too-large, name-too-long, invalid-read-access, no-file, fault]
  - page: file-description
    states: [default, refused, not-found]
  - page: file-download
    states: [default, refused, not-found]
  - page: file-attachment-control
    states: [default, empty, new-attachment, invalid, too-large, public-view]
  - page: file-image-picker
    states: [default, empty, chosen, rejected]
  - page: file-embedded-image
    states: [default, uploading, inserted, failed, published]

  # proposals domain — see design/DESIGN.md, "Domain: proposals"
  - page: proposal-cwu-create
    states: [default, organization, invalid, refused, terms, not-found]
  - page: proposal-swu-create
    states: [default, invalid, unqualified-organization, refused, terms, not-found]
  - page: proposal-twu-create
    states: [default, invalid, over-budget, unqualified-organization, refused, terms, not-found]
  - page: proposal-cwu-edit
    states:
      [
        default,
        draft,
        editing,
        terms,
        delete-confirm,
        withdraw-confirm,
        submit-refused,
        awarded,
        history-tab,
        not-found,
        loading,
      ]
  - page: proposal-swu-edit
    states:
      [
        default,
        draft,
        editing,
        organization-locked,
        terms,
        delete-confirm,
        withdraw-confirm,
        submit-refused,
        scoresheet-tab,
        history-tab,
        not-found,
        loading,
      ]
  - page: proposal-twu-edit
    states:
      [
        default,
        draft,
        editing,
        organization-locked,
        terms,
        delete-confirm,
        withdraw-confirm,
        submit-refused,
        scoresheet-tab,
        history-tab,
        not-found,
        loading,
      ]
  - page: proposal-cwu-view
    states:
      [
        default,
        score-dialog,
        score-invalid,
        evaluated,
        award-confirm,
        disqualify-dialog,
        disqualify-invalid,
        history-tab,
        not-found,
        loading,
      ]
  - page: proposal-swu-view
    states:
      [
        default,
        team-questions-tab,
        code-challenge-tab,
        score-dialog,
        team-scenario-tab,
        wrong-stage,
        evaluated,
        award-confirm,
        disqualify-dialog,
        history-tab,
        not-found,
        loading,
      ]
  - page: proposal-twu-view
    states:
      [
        default,
        resource-questions-tab,
        challenge-tab,
        score-dialog,
        wrong-stage,
        evaluated,
        award-confirm,
        disqualify-dialog,
        history-tab,
        not-found,
        loading,
      ]
  - page: proposal-cwu-export-one
    states: [default, not-found, loading]
  - page: proposal-swu-export-one
    states: [default, anonymous, not-found, loading]
  - page: proposal-twu-export-one
    states: [default, anonymous, not-found, loading]
  - page: proposal-cwu-export-all
    states: [default, anonymous, not-found, loading]
  - page: proposal-swu-export-all
    states: [default, anonymous, not-found, loading]
  - page: proposal-twu-export-all
    states: [default, anonymous, not-found, loading]
  - page: proposal-vendor-dashboard
    states: [default, no-organization, empty, loading]
  - page: proposal-list-stub
    states: [default]

  # evaluation domain — see design/DESIGN.md, "Domain: evaluation"
  - page: evaluation-panel-dashboard
    states: [default, empty, loading]
  - page: evaluation-panel-swu
    states: [default, invalid, too-few, refused, locked, not-found, loading]
  - page: evaluation-panel-twu
    states: [default, invalid, too-few, refused, locked, not-found, loading]
  - page: evaluation-instructions-swu
    states: [default, not-found, loading]
  - page: evaluation-instructions-twu
    states: [default, not-found, loading]
  - page: evaluation-individual-list-swu
    states: [default, ready, submitted, refused, not-found, loading]
  - page: evaluation-individual-list-twu
    states: [default, ready, submitted, refused, not-found, loading]
  - page: evaluation-consensus-list-swu
    states:
      [
        default,
        ready,
        submit-confirm,
        submitted,
        finalize-confirm,
        not-all-submitted,
        no-screenable,
        withheld,
        not-found,
        loading,
      ]
  - page: evaluation-consensus-list-twu
    states:
      [
        default,
        ready,
        submit-confirm,
        submitted,
        finalize-confirm,
        not-all-submitted,
        no-screenable,
        withheld,
        not-found,
        loading,
      ]
  - page: evaluation-individual-create-swu
    states: [default, invalid, duplicate, not-found, loading]
  - page: evaluation-individual-edit-swu
    states: [default, invalid, submitted, not-found, loading]
  - page: evaluation-consensus-create-swu
    states: [default, invalid, duplicate, chair-only, not-found, loading]
  - page: evaluation-consensus-edit-swu
    states: [default, submitted, invalid, not-found, loading]
  - page: evaluation-individual-create-twu
    states: [default, invalid, duplicate, not-found, loading]
  - page: evaluation-individual-edit-twu
    states: [default, invalid, submitted, not-found, loading]
  - page: evaluation-consensus-create-twu
    states: [default, invalid, duplicate, chair-only, not-found, loading]
  - page: evaluation-consensus-edit-twu
    states: [default, submitted, invalid, not-found, loading]
